# Copy this file to .env and fill in your actual API keys
# This file shows the expected format for environment variables

# Bybit API Credentials
BYBIT_API_KEY=your_bybit_api_key_here
BYBIT_API_SECRET=your_bybit_api_secret_here

# Binance API Credentials (optional)
BINANCE_API_KEY=your_binance_api_key_here
BINANCE_API_SECRET=your_binance_api_secret_here

# Notes:
# - Never commit the actual .env file with real API keys to version control
# - The .env file is already in .gitignore to prevent accidental commits
# - API keys are only needed for private endpoints (account info, trading)
# - Public endpoints (market data, prices) work without API keys
